import 'package:flutter/material.dart';

class HardCodedText extends StatelessWidget {
  const HardCodedText({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("Click the button to fetch data");
  }
}